import { Button, StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    Button: {
        backgroundColor: "#00f",
        padding: 16,
        borderRadius: 8,
        margin: 16,
    },
    title: {
        color: "#00f",
        fontSize: 24,
        fontWeight: "bold",
    },
});